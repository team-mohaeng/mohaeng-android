<?xml version="1.0" encoding="utf-8"?>
<layout>
    <data>
        <variable
            name="data"
            type="org.journey.android.onboarding.data.OnboardingCourseEntity" />
    </data>
<androidx.constraintlayout.widget.ConstraintLayout
    android:id="@+id/layout_background"
    android:layout_width="match_parent"
    android:layout_height="106dp"
    android:layout_marginBottom="20dp"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:android="http://schemas.android.com/apk/res/android">
    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/image_background"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        loadDrawable="@{data.background}"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>
    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/textview_course_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@{data.courseName}"
        android:fontFamily="@font/gmarketsansttfmedium"
        android:textSize="20sp"
        android:textColor="@color/black"
        android:layout_marginTop="28dp"
        android:layout_marginStart="25dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <androidx.appcompat.widget.AppCompatTextView
        android:id="@+id/textview_course_explanation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="@font/spoqahansansneomedium"
        android:textSize="12sp"
        android:textColor="@color/black"
        android:text="@{data.courseExplanation}"
        android:layout_marginTop="20dp"
        app:layout_constraintTop_toBottomOf="@id/textview_course_name"
        app:layout_constraintStart_toStartOf="@id/textview_course_name"/>

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/imageview_course_icon"
        android:layout_width="68dp"
        android:layout_height="68dp"
        android:layout_marginEnd="10dp"
        loadDrawable="@{data.courseIcon}"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"/>

</androidx.constraintlayout.widget.ConstraintLayout>
</layout>